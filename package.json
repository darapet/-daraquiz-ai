{
  "name": "daraquiz-ai",
  "version": "1.0.0",
  "description": "DaraQuiz AI — Smart Quiz System with AI Studio",
  "scripts": {
    "build": "echo 'Static site — no build step needed' && exit 0",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@codetrix-studio/capacitor-google-auth": "^3.4.0"
  },
  "devDependencies": {}
}
